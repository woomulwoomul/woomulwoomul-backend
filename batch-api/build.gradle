plugins {
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-jooq'
}

test {
    useJUnitPlatform()
    finalizedBy 'jacocoTestReport'
}

bootJar {
    archiveFileName = 'batch-app.jar'
}